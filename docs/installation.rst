============
Installation
============

At the command line::

    $ easy_install object_view

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv object_view
    $ pip install object_view